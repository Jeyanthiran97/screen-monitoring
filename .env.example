# ============================================
# Screen Monitoring System - Environment Variables
# ============================================
# Copy this file to .env.local and update with your values
# Command: cp .env.example .env.local
# NEVER commit .env.local to version control

# ============================================
# Database Configuration
# ============================================
# MongoDB connection string
# Local: mongodb://localhost:27017/screen-monitoring
# MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/screen-monitoring
# Docker: mongodb://mongo:27017/screen-monitoring
MONGODB_URI=mongodb://localhost:27017/screen-monitoring

# ============================================
# Authentication & Security
# ============================================
# JWT Secret Key - CHANGE THIS IN PRODUCTION!
# Generate a strong random string (minimum 32 characters)
# You can generate one using: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-this-in-production-minimum-32-characters-long

# JWT Token Expiration Time
# Format: number + unit (e.g., 7d, 24h, 60m, 3600s)
# Default: 7d (7 days)
JWT_EXPIRES_IN=7d

# ============================================
# Application URLs
# ============================================
# Public application URL (used for CORS and Socket.IO)
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Socket.IO Server URL (usually same as APP_URL)
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_SOCKET_URL=http://localhost:3000

# ============================================
# Server Configuration
# ============================================
# Server hostname (default: localhost)
HOSTNAME=localhost

# Server port (default: 3000)
PORT=3000

# Node Environment
# Options: development, production, test
NODE_ENV=development

# ============================================
# WebRTC Configuration (Optional - for Internet Mode)
# ============================================
# STUN Server (Google's public STUN server is used by default)
# You can use your own STUN server if needed
# NEXT_PUBLIC_STUN_SERVER=stun:stun.l.google.com:19302

# TURN Server Configuration (Required for production Internet mode)
# Uncomment and configure if you have a TURN server
# NEXT_PUBLIC_TURN_SERVER=turn:your-turn-server.com:3478
# NEXT_PUBLIC_TURN_USERNAME=your-turn-username
# NEXT_PUBLIC_TURN_CREDENTIAL=your-turn-password

# ============================================
# Production Settings (Optional)
# ============================================
# Enable HTTPS in production (set to true if using HTTPS)
# SECURE_COOKIES=true

# Session cookie domain (for production)
# COOKIE_DOMAIN=.yourdomain.com

# ============================================
# Development Settings
# ============================================
# Enable debug logging (set to true for verbose logs)
# DEBUG=false
